
<problem>
    <script>
from math import factorial as f
from math import ceil
import random

def C(n,m):
    return f(n)/f(m)/f(n-m)

def myrandom(start,stop,step=1):
    return random.randint(0, int((stop - start) / step)) * step + start







answer1 = "0.33"
answer2 = "0.33"
</script>
<p><br/>
<br/>
In the last example, we implicitly used a summation rule. Suppose \(A_1, \ldots, A_k \subset \Omega\) are disjoint events whose union is \(\Omega\) (that is, exactly one of them will occur). Then for any event \(E \subset \Omega\) <br/>
\( P(E) <br/>
\ = \ <br/>
\sum_{i=1}^k P(E \cap A_i) <br/>
\ = \ <br/>
\sum_{i=1}^k P(E | A_i) P(A_i).<br/>
\)<br/>
(If the \(A_i\) are not disjoint, simply replace the first equality with a \(\leq\) ) We will often write \(P(E \cap A_i)\) as \(P(E,A_i)\) <br/>
Let events \(X, Y, Z, W\) satisfy:<br/>
o  \(X \cap Y \cap Z = \emptyset\)<br/>
o  \(X \cup Y \cup Z = \Omega = \) outcome space<br/>
o  \(P(X \cap W) = 0.1\)<br/>
o  \(P(Y \cap W) = 0.2\)<br/>
o  \(P(W | Z) = 0.3\)<br/>
o  \(P(Z) = 0.1\)<br/>
o  \(P(W) = \)? <formularesponse type="ci" samples="n,m@1,1:20,20#10" answer="$answer1">
                    <responseparam type="tolerance" default="0.00001"/>
                    <formulaequationinput />
                </formularesponse>
<br/>
</p>
</problem>